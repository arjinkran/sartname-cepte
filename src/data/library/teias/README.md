# teias — TEİAŞ belgeleri

Henüz katalogda gerçek bir belge yok (bkz. `documents.ts` — boş dizi).
Veri Kaynakları ekranında bu kurum "Doküman kütüphanesi yakında
eklenecek" olarak gösterilir (`repository.getStatistics()` sayımı 0
döner).

## Yeni belge ekleme

`../tedas/README.md`'deki adımlarla aynıdır — ortak `Document` modeli
(`../types.ts`) kullanılır, mock/uydurma belge eklenmez.
