// sag motoru — hava hattı iletkeninde sehim (sag) hesabı.
// Sprint 2A: yalnızca iskelet. Girdi/çıktı alanları Excel analizinden
// sonra tanımlanacak, gerçek motor (engine.ts) o zaman eklenecektir.

/** Excel analizinden sonra tanımlanacak — şimdilik boş girdi kümesi. */
export type SagInput = Record<string, never>;

/** Excel analizinden sonra tanımlanacak — şimdilik boş çıktı kümesi. */
export type SagOutput = Record<string, never>;
